name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  quality:
    name: Quality (${{ matrix.os }}, py${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.10", "3.12", "3.14"]

    env:
      PIP_DISABLE_PIP_VERSION_CHECK: "1"
      MNEME_RELEASE_TAG: v0.5.0
      MNEME_AUTO_DOWNLOAD: "1"
      GITHUB_TOKEN: ${{ github.token }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: python/pyproject.toml

      - name: Verify python README is in sync
        run: python scripts/sync_python_readme.py --check

      - name: Cache mneme native release artifacts
        uses: actions/cache@v5
        with:
          path: ~/.cache/mneme-python
          key: mneme-native-${{ runner.os }}-py${{ matrix.python-version }}-${{ env.MNEME_RELEASE_TAG }}

      - name: Install package and dev dependencies
        run: python -m pip install -e './python[dev]'

      - name: Warm native library cache
        run: |
          python - <<'PY'
          import mneme.native as n
          print(n._download_release_library())
          PY

      - name: Ruff lint
        run: python -m ruff check python/src python/tests

      - name: Ruff format check
        run: python -m ruff format --check python/src python/tests

      - name: Bandit
        run: python -m bandit -c python/pyproject.toml -r python/src

      - name: MyPy
        run: python -m mypy --config-file python/pyproject.toml python/src/mneme

      - name: Pytest + coverage gate
        run: python -m pytest python/tests

  package:
    name: Package build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: python/pyproject.toml

      - name: Verify python README is in sync
        run: python scripts/sync_python_readme.py --check

      - name: Build and check sdist/wheel
        working-directory: python
        run: |
          python -m pip install build twine
          python -m build
          python -m twine check dist/*
